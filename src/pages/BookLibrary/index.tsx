import { Text } from "react-native";
import { Container } from "./styles";

interface BookLibraryProps {}

export function BookLibrary({}: BookLibraryProps) {
	return (
		<Container>
			<Text>Testando</Text>
		</Container>
	);
}
