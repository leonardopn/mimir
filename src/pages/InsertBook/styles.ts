import { RFValue } from "react-native-responsive-fontsize";
import styled from "styled-components/native";

export const InsertBookContainerDefault = styled.View`
	padding: ${RFValue(20)}px;
	flex: 1;
`;
